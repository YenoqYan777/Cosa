<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/mainBckg"
        android:clickable="true"
        android:focusable="true"
        tools:context=".arch.settings.SettingsFragment">


        <TextView
            android:id="@+id/textView2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/language"
            android:textColor="@color/textColor"
            android:textSize="25sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <View
            android:id="@+id/view"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#ffffff"
            android:elevation="2dp"
            app:layout_constraintBottom_toTopOf="@+id/saveInTrash" />

        <Switch
            android:id="@+id/saveInTrash"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:text="@string/save_deleted_items_in_trash"
            android:textColor="@color/textColor"
            android:textSize="22dp"
            app:layout_constraintEnd_toEndOf="@+id/langListSettings"
            app:layout_constraintStart_toStartOf="@+id/langListSettings"
            app:layout_constraintTop_toBottomOf="@+id/langListSettings" />

        <ListView
            android:id="@+id/langListSettings"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/textView2">

        </ListView>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>